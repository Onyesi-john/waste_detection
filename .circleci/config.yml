version: 2.1

jobs:
  clone_repository:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout

  setup_python_environment:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout
      - run:
          name: Set Up Python Environment
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt
      - persist_to_workspace:
          root: /home/circleci/project
          paths:
            - .  # Persist the entire workspace

  train_model:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout
      - attach_workspace:
          at: /home/circleci/project
      - run:
          name: Train Model
          command: |
            . venv/bin/activate
            cd yolov5
            python train.py --data ../dataset/data.yaml --weights yolov5nu.pt --epochs 50 --project ../runs --name exp --exist-ok
      - run:
          name: Ensure Best Model Exists
          command: |
            if [ ! -f /home/circleci/project/runs/exp/weights/best.pt ]; then
              echo "Training failed: best.pt not found!"
              exit 1
            fi
      - persist_to_workspace:
          root: /home/circleci/project
          paths:
            - runs/exp/weights/best.pt  # Persist only the model

  build_and_push_docker_image:
    docker:
      - image: docker:latest
    steps:
      - checkout
      - attach_workspace:
          at: /home/circleci/project
      - setup_remote_docker
      - run:
          name: Verify Model Exists Before Docker Build
          command: |
            if [ ! -f /home/circleci/project/runs/exp/weights/best.pt ]; then
              echo "Error: best.pt not found!"
              exit 1
            fi
      - run:
          name: Move Best Model to Docker Build Context
          command: |
            mkdir -p /home/circleci/project/docker_model
            cp /home/circleci/project/runs/exp/weights/best.pt /home/circleci/project/docker_model/
      - run:
          name: Log in to Docker Hub
          command: |
            echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
      - run:
          name: Build and Push Docker Image
          command: |
            docker build -t $DOCKERHUB_USERNAME/waste_detection:latest /home/circleci/project
            docker push $DOCKERHUB_USERNAME/waste_detection:latest

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - clone_repository
      - setup_python_environment:
          requires:
            - clone_repository
      - train_model:
          requires:
            - setup_python_environment
      - build_and_push_docker_image:
          requires:
            - train_model