version: 2.1

jobs:
  clone_repository:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout

  setup_python_environment:
    docker:
      - image: cimg/python:3.9  
    steps:
      - checkout
      
      - run:
          name: Set Up Python Environment
          command: |
            cd ultralytics  # Move inside the cloned repo
            apt-get update && apt-get install -y python3-venv  
            python3 -m venv venv
            . venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt

      - persist_to_workspace:
          root: .
          paths:
            - ultralytics  # Persist the whole repository

  train_model:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout
      - attach_workspace:
          at: .
     
      - run:
          name: Train Model
          command: |
            cd ultralytics  # Ensure training happens inside the repo
            . venv/bin/activate
            python train.py

      - run:
          name: Ensure Best Model Exists
          command: |
            ls -lah ultralytics/runs/train/exp/weights/
            if [ ! -f ultralytics/runs/train/exp/weights/best.pt ]; then
              echo "Training failed: best.pt not found!"
              exit 1
            fi

      - persist_to_workspace:
          root: .
          paths:
            - ultralytics/runs/train/exp/weights/best.pt  # Persist the trained model

  build_and_push_docker_image:
    docker:
      - image: docker:latest  
    steps:
      - checkout
      - attach_workspace:
          at: .
      - setup_remote_docker
      - run:
          name: Authenticate with Docker Hub
          command: |
            echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin 2>/dev/null || { echo "Docker login failed!"; exit 1; }
      - run:
          name: Move Best Model to Docker Build Context
          command: |
            mkdir -p docker_model
            cp ultralytics/runs/train/exp/weights/best.pt docker_model/  # Copy best.pt for Docker

      - run:
          name: Build and Push Docker Image
          command: |
            docker build -t $DOCKERHUB_USERNAME/waste_detection:latest .
            docker images
            docker push $DOCKERHUB_USERNAME/waste_detection:latest

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - clone_repository
      - setup_python_environment:
          requires:
            - clone_repository
      - train_model:
          requires:
            - setup_python_environment
      - build_and_push_docker_image:
          requires:
            - train_model
